# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — never commit secrets
.env
.env.local
.env.*.local
.env.development
.env.production

# vercel
.vercel

# prisma
/prisma/*.db

# SQLite database + logs + backups (contain client credentials — never commit)
/data
/logs
/backups
/.local-pg

# local tooling
/.claude
/deploy/node_modules
/deploy/package-lock.json
/deploy/.vps.env
/install.log
